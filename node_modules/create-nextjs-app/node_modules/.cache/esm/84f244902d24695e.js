let childProcess;_2d2‍.x([["default",()=>_2d2‍.o]]);_2d2‍.w("child_process",[["default",["childProcess"],function(v){childProcess=v}]]);

const spawn = (command, args) =>
  new Promise((resolve, reject) => {
    const child = childProcess.spawn(command, args, {
      cwd: process.cwd(),
      stdio: 'inherit',
    });

    child.on('exit', () => {
      resolve();
    });
    child.on('error', err => {
      reject(err);
    });
  });

_2d2‍.d(spawn);
